from conf import machines_names
from Machines import Machine

# def namestr(obj, namespace):
#     return [name for name in namespace if namespace[name] is obj][0]
#
# master_on = 1
# print(namestr(master_on, globals()))

mas = {'split': 1}
print(mas[0])